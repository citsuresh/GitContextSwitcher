<Window x:Class="GitContextSwitcher.UI.Views.SaveContextDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Work Context" Height="180" Width="420" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Enter a short description for this saved work context:" FontWeight="SemiBold" />
        <TextBox x:Name="DescriptionBox" Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap" Height="60" Margin="0,8,0,8" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OkButton" Width="140" Margin="6,0,0,0" Content="Save" IsDefault="True" Click="OkButton_Click" />
            <Button Width="84" Margin="6,0,0,0" Content="Cancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>