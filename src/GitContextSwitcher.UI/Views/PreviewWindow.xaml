<Window x:Class="GitContextSwitcher.UI.Views.PreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:GitContextSwitcher.UI.Views"
        Title="Preview Saved Context" Height="600" Width="800" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Padding="8" Background="#FFF" BorderBrush="#FFDDDDDD" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Context details" FontWeight="SemiBold" />
                <Border BorderBrush="#EEE" BorderThickness="1" Padding="6" Margin="0,6,0,0" Background="#FFF">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Height="160">
                        <TreeView ItemsSource="{Binding ContextProperties}">
                            <TreeView.ItemTemplate>
                                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBox Text="{Binding Name, Mode=OneWay}" FontWeight="SemiBold" IsReadOnly="True" IsReadOnlyCaretVisible="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" VerticalAlignment="Center" />
                                        <TextBlock Text=": " />
                                        <TextBox Text="{Binding Value, Mode=OneWay}" IsReadOnly="True" IsReadOnlyCaretVisible="True" BorderThickness="0" Background="Transparent" Padding="0" Margin="0" VerticalAlignment="Center" />
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                            </TreeView.ItemTemplate>
                        </TreeView>
                    </ScrollViewer>
                </Border>
            </StackPanel>
        </Border>

        <views:PreviewPane Grid.Row="1" x:Name="InnerPreviewPane" Margin="8" />
    </Grid>
</Window>

